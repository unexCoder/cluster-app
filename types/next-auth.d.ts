// NextAuth type definitions - DEPRECATED
// This project now uses JWT-based authentication via custom API routes
// These types are no longer used but kept for reference

// Legacy NextAuth module declarations (no longer used)
// declare module "next-auth" { ... }
// declare module "next-auth/jwt" { ... }
